# Publication plan Shorts → clip (yt-dlp) + upload OAuth — self-host, ToS / droits à respecter
ENABLE_AUTO_PUBLISH=true
ENABLE_CLIP_DOWNLOAD=true

# Si les binaires ne sont pas sur le PATH, décommente et adapte :
# FFMPEG_PATH=ffmpeg
# YTDLP_PATH=yt-dlp
